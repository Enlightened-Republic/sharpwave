{
  "name": "sharpwave-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build --workspace sharpwave-core && npm run build --workspace sharpwave",
    "test": "npm run build --workspace sharpwave-core && npm run test --workspace sharpwave-core",
    "test:mcp": "npm run build --workspace sharpwave-core && npm run build --workspace sharpwave && npm run test:mcp --workspace sharpwave",
    "test:all": "npm run test && npm run test:mcp",
    "test:pack": "node packages/mcp/test/pack-smoke.mjs",
    "typecheck": "tsc -b packages/core packages/mcp"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
